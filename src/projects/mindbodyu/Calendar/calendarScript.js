// Function to add a task based on user input from a form
function addTask(formObj){
    var current= document.getElementById("dayOfWeek");
    var day = current.option[current.selectedIndex].text;
    var task = document.getElementById("task_name");
    //alert(day);
}